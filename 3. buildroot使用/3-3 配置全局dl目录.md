## 1、配置全局工具链
### 1. 编译SDK
```bash
sudo mkdir -p /opt/buildroot/download
```
```bash
sudo mkdir -p /opt/buildroot/download
```
### 1.2 安装到服务器
```bash
sudo mkdir -p /opt/buildroot/download
```
## 2、配置全局dl目录
### 2.1 修改defconfig目录
```bash
sudo mkdir -p /opt/buildroot/download
```
### 2.1 创建目录
```bash
sudo mkdir -p /opt/buildroot/download
```
### 2.2 设置所属组为users【也可设置为对应的开发组】
```bash
sudo chown -R :users /opt/buildroot/download
```
### 2.3 设置download目录权限为全局可读写
```bash
sudo chmod 2775 /opt/buildroot/download
```