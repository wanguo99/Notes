1. 下载源码
```bash
git clone --recurse-submodules https://github.com/nasa/cFS.git
```
2. 编译
```bash
make distclean # (For a clean build on subsequent runs)
make SIMULATION=native prep
make
make install
cd build/exe/cpu1/
./core-cpu1
```

