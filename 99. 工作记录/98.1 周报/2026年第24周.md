# 周期：【2026-06-06 (周六) ~ 2026-06-12 (周五)】

## 1. Task List

2026-06-06 周六
- [x] 构建框架全局启用-Werror，确保不允许出现任何编译问题
- [x] 修复osal_log重构引入的编译问题
- [x] 将编译工具链、OS、ARCH改成使用kconfig配置
- [x] 将所有的cmake宏参数全都在kconfig中进行配置
- [x] 更新所有的defconfig
- [x] osal.h、hal.h、prl.h等核心头文件需要支持根据功能配置进行裁剪
- [x] 使用不透明指针等方式，封装内部实现
- [x] 移除cmake中对于${SDK_PATH}/core/types/include的引用
- [x] 重构tests，测试模块也应该根据defconfig中开启的功能，显示不同的内容，而不是写死
- [x] 删除过时的makefile

---

2026-06-07 周日
- [x] 筛查非法调用标准库和系统调用的问题
- [x] hal配置使用联合体
- [x] BMC支持主备双通道配置 
- [x] pdl_types.h拆分到各个模块内部
- [x] 应该根据defconfig生成一个kconfig.h并全局包含，实现代码内功能裁剪
- [x] 为三个综合性能测试文件添加对应的 Kconfig 配置项
- [x] 筛查头文件循环包含的问题

---

2026-06-08 周一
- [x] OSAL确保所有的系统调用和标准库接口使用OSAL_前缀+函数原名的形式，降低学习成本
- [x] 重构tests测试用例实现形式，改用函数定义的方式，而不是宏定义，方便代码阅读
- [x] 更新NAS上Gitea容器配置，支持ssh访问仓库，搭建Gitea仓库
- [x] 上传原始版本SDK压缩包文件到NAS
- [x] 上传AM62x开发源码【buildroot、buildroot-external-TI、u-boot、kernel、ti-linux-firmware】
- [x] 上传buildroot download目录压缩包到NAS
- [x] 移植buildroot-external-TI、kernel修改到Gitea仓库代码
- [x] Gitea仓库代码编译通过

---

2026-06-09 周二
- [x] 解决编译错误：./build.py clean && ./build.py build --config tests_x86_full_defconfig
- [x] 解决非法包含标准库的问题：./products/tests/core/test_runner.c:9:#include <stdarg.h>
- [x] 更新工程配置文件名以及产品配置文件名，使用h200_100p_am625作为项目名
- [x] 重构tests测试用例实现形式，改用函数定义的方式，而不是宏定义，方便代码阅读
- [x] buildroot external中增加busybox独立配置，并开启TOP_SMP相关功能
- [x] 规划HWID字段
- [x] 需要支持获取HWID，并根据HWID使用不同的配置
- [x] 筛查头文件包含头文件的情况，仅在核心头文件中允许包含其他头文件
- [x] 根据依赖关系，更新所有文件中的头文件包含顺序
- [x] 创建initramfs根文件系统配置，并裁剪busybox，实现最小系统启动
	- [x] 确定使用ash，bash是一个独立的开源库，需要单独移植，先链接到ash使用
	- [x] 支持telnet、tftp、ifconfig
	- [x] 支持tar、vi
	- [x] 支持top（SMP支持）
	- [x] 支持fdisk, mkfs, fsck
	- [x] 支持i2c-tools、hexdump



---

2026-06-10 周三
- [x] 将 CRC 校验功能从 PRL 提取到 OSAL
- [x] rename EMS to ES-Middleware
- [x] 删除误传的github、gitab的CI配置文件
- [x] 删除重复或冗余的头文件，清理头文件重复内容，重点是内部私有头文件
- [x] 关闭rootfs tar和squashfs生成
- [x] 裁剪initramfs中的perf、bash、eudev
- [x] 裁剪am62x_sdk_defconfig
- [x] buildroot使用服务器全局dl目录和SDK工具链，提升效率
- [x] 归档buildroot SDK到NAS的03-项目文件\00-SDK\目录
- [x] 内核开启CAN功能
- [x] 内核dts开启CAN节点，ifconfig可以识别到can0设备
- [x] 新增三方板 buildroot配置
- [x] 新增三方板内核dts配置
- [x] 三方板单独适配sdcard，可以正常识别到SD卡


---

2026-06-11 周四
- [x] 排查内核开启CAN功能失败的问题【CONFIG_PHY_CAN_TRANSCEIVER配置未同步提交，导致开启失败】
- [x] 内核dts开启MCU域的两路CAN节点，ifconfig可以识别到can1、can2设备
- [x] 重构ES-Middleware构建框架，弃用kconfiglib，移植buildroot内的kconfig，并改成cmake编译
- [x] 重构ES-Middleware构建框架，增加根目录Makefile，在Makefile中套用cmake，实现内核风格的工作流，降低学习成本，同时保留cmake的高性能
- [x] 完全移除ES-Middleware中对Python的依赖

---

2026-06-12 周五
- [x] OSAL_log的报错不应该使用强制类型转换解决【(void)write(STDOUT_FILENO, console_buf, console_len); 】【cast write() return value to void】
- [ ] 移植busybox的kconfig到ES-Middleware
- [ ] kconfig改成Config.in
- [ ] 将ES-Middleware适配到buildroot框架
- [ ] 解决编译报错问题
- [ ] 解决不支持交叉编译的问题
- [ ] 解决存在的编译告警
- [ ] 编译目录不应该写死_build，应该支持配置，兼容buildroot
- [ ] 清理框架中涉及到Python的脚本和描述性内容
- [ ] 分析并解决```make all时 CMAKE _build```卡顿无输出的问题【实际是在编译kconfig工具】，优化kconfig编译速度
- [ ] 分析一下01-kconfig-kernel-to-buildroot.patch这个文件中的修改
- [!] .config应该是基于defconfig生成，而不是直接拷贝【```cp "$$config" $(KCONFIG_CONFIG)```】，分析并参考buildroot的Makefile进行优化
- [ ] 增加make mrproper支持，默认clean不清理kconfig
- [ ] 验证所有的defconfig都可以正常清理、配置和编译，并进行压力测试
- [ ] 把OSAL的平台相关的宏全都放到defconfig中定义
- [ ] 把OSAL的功能宏也移动到defconfig中
- [ ] 确认clean和distclean是否重编了kconfig
- [ ] 整体优化构建框架，写得更优雅一些
- [ ] CONFIG_OSAL_OS_POSIX改成被OS配置select
- [ ] 版本号支持custom，而不是固定写死，也不要用VERSION文件手动修改，改成类似buildroot的形式，获取分支或git commit id的方式
- [ ] 考虑configs增加一级子目录
- [ ] products/ccm/configs/platforms/h200_100p_am625目录下的aconfig和pconfig考虑重构目录结构
- [ ] distclean没有删除.config.old
- [ ] product内的库链接应该是根据defconfig进行配置的，而不是默认包含
- [ ] 优化core库的构建和链接机制

## [[README#98-2 非紧急待办|2. 其他待办]]
## [[README#98-3 项目开发|3. 项目开发]]
## [[README#6. 问题记录|4. 问题记录]]
