# 周期：【2026-06-06 (周六) ~ 2026-06-12 (周五)】

## 1. Task List

2026-06-06 周六
- [x] 构建框架全局启用-Werror，确保不允许出现任何编译问题
- [x] 修复 osal_log 重构引入的编译问题
- [x] 将编译工具链、OS、ARCH 改成使用 kconfig 配置
- [x] 将所有的 cmake 宏参数全都在 kconfig 中进行配置
- [x] 更新所有的 defconfig
- [x] osal.h、hal.h、prl.h 等核心头文件需要支持根据功能配置进行裁剪
- [x] 使用不透明指针等方式，封装内部实现
- [x] 移除 cmake 中对于${SDK_PATH}/core/types/include 的引用
- [x] 重构 tests，测试模块也应该根据 defconfig 中开启的功能，显示不同的内容，而不是写死
- [x] 删除过时的 makefile

---

2026-06-07 周日
- [x] 筛查非法调用标准库和系统调用的问题
- [x] hal 配置使用联合体
- [x] BMC 支持主备双通道配置 
- [x] pdl_types.h 拆分到各个模块内部
- [x] 应该根据 defconfig 生成一个 kconfig.h 并全局包含，实现代码内功能裁剪
- [x] 为三个综合性能测试文件添加对应的 Kconfig 配置项
- [x] 筛查头文件循环包含的问题

---

2026-06-08 周一
- [x] OSAL 确保所有的系统调用和标准库接口使用 OSAL_前缀+函数原名的形式，降低学习成本
- [x] 重构 tests 测试用例实现形式，改用函数定义的方式，而不是宏定义，方便代码阅读
- [x] 更新 NAS 上 Gitea 容器配置，支持 ssh 访问仓库，搭建 Gitea 仓库
- [x] 上传原始版本 SDK 压缩包文件到 NAS
- [x] 上传 AM62x 开发源码【buildroot、buildroot-external-TI、u-boot、kernel、ti-linux-firmware】
- [x] 上传 buildroot download 目录压缩包到 NAS
- [x] 移植 buildroot-external-TI、kernel 修改到 Gitea 仓库代码
- [x] Gitea 仓库代码编译通过

---

2026-06-09 周二
- [x] 解决编译错误：./build.py clean && ./build.py build --config tests_x86_full_defconfig
- [x] 解决非法包含标准库的问题：./products/tests/core/test_runner.c:9:#include <stdarg.h>
- [x] 更新工程配置文件名以及产品配置文件名，使用 h200_100p_am625 作为项目名
- [x] 重构 tests 测试用例实现形式，改用函数定义的方式，而不是宏定义，方便代码阅读
- [x] buildroot external 中增加 busybox 独立配置，并开启 TOP_SMP 相关功能
- [x] 规划 HWID 字段
- [x] 需要支持获取 HWID，并根据 HWID 使用不同的配置
- [x] 筛查头文件包含头文件的情况，仅在核心头文件中允许包含其他头文件
- [x] 根据依赖关系，更新所有文件中的头文件包含顺序
- [x] 创建 initramfs 根文件系统配置，并裁剪 busybox，实现最小系统启动
	- [x] 确定使用 ash，bash 是一个独立的开源库，需要单独移植，先链接到 ash 使用
	- [x] 支持 telnet、tftp、ifconfig
	- [x] 支持 tar、vi
	- [x] 支持 top（SMP 支持）
	- [x] 支持 fdisk, mkfs, fsck
	- [x] 支持 i2c-tools、hexdump

---

2026-06-10 周三
- [x] 将 CRC 校验功能从 PRL 提取到 OSAL
- [x] rename EMS to ES-Middleware
- [x] 删除误传的 github、gitab 的 CI 配置文件
- [x] 删除重复或冗余的头文件，清理头文件重复内容，重点是内部私有头文件
- [x] 关闭 rootfs tar 和 squashfs 生成
- [x] 裁剪 initramfs 中的 perf、bash、eudev
- [x] 裁剪 am62x_sdk_defconfig
- [x] buildroot 使用服务器全局 dl 目录和 SDK 工具链，提升效率
- [x] 归档 buildroot SDK 到 NAS 的 03-项目文件\00-SDK\目录
- [x] 内核开启 CAN 功能
- [x] 内核 dts 开启 CAN 节点，ifconfig 可以识别到 can0 设备
- [x] 新增三方板 buildroot 配置
- [x] 新增三方板内核 dts 配置
- [x] 三方板单独适配 sdcard，可以正常识别到 SD 卡

---

2026-06-11 周四
- [x] 排查内核开启 CAN 功能失败的问题【CONFIG_PHY_CAN_TRANSCEIVER 配置未同步提交，导致开启失败】
- [x] 内核 dts 开启 MCU 域的两路 CAN 节点，ifconfig 可以识别到 can1、can2 设备
- [x] 重构 ES-Middleware 构建框架，弃用 kconfiglib，移植 buildroot 内的 kconfig，并改成 cmake 编译
- [x] 重构 ES-Middleware 构建框架，增加根目录 Makefile，在 Makefile 中套用 cmake，实现内核风格的工作流，降低学习成本，同时保留 cmake 的高性能
- [x] 完全移除 ES-Middleware 中对 Python 的依赖

---

2026-06-12 周五
- [x] OSAL_log 的报错不应该使用强制类型转换解决【(void)write(STDOUT_FILENO, console_buf, console_len); 】【cast write() return value to void】
- [x] 移植 busybox 的 kconfig 到 ES-Middleware
- [x] 将 ES-Middleware 适配到 buildroot 框架
	- [x] .config 应该是基于 defconfig 生成，而不是直接拷贝【```cp "$$config" $(KCONFIG_CONFIG)```】，分析并参考 busybox 的 Makefile 进行优化
	- [x] 解决编译报错问题
	- [x] 解决不支持交叉编译的问题
	- [x] 解决存在的编译告警
	- [x] 编译目录不应该写死_build，应该支持配置，兼容 buildroot
	- [x] 分析并解决```make all 时 CMAKE _build```卡顿无输出的问题【实际是在编译 kconfig 工具】

## [[README#98-2 非紧急待办|2. 其他待办]]

## [[README#98-3 项目开发|3. 项目开发]]

## [[README#6. 问题记录|4. 问题记录]]
