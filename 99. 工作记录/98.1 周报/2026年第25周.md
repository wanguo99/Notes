## 1. Task List：

2026-06-13 周六
- [x] 整体优化构建框架，写得更优雅一些
- [x] ES-Middleware测试程序已经可以在开发板上运行
- [x] 排查为什么ES-Middleware会触发两次编译【install依赖all，导致make install的时候会重复触发make all】
- [x] kconfig改成Config.in，重命名文件，并更新文件中的引用
- [x] 根目录的Config.in合并到一个Config.in内
- [x] 删除scripts/kconfig.backup以及其他废弃的构建文件和非必要的文档
- [x] 清理框架内脚本和文档中涉及到Python的内容
- [x] buildroot-external/package内的es-middleware需要默认使用git仓库源码，支持local.mk配置本地源码编译
- [x] 删除ES_MIDDLEWARE_INSTALL_STAGING功能
- [x] 把tools/cmake/移动到scripts或者kconfig移动到tools，分析一下哪种做法更合适
- [x] OSAL缺少sizeof封装，在OSAL内补充实现，
- [x] 替换scripts以外所有sizeof相关的引用
- [x] 把所有的OSAL_SIZEOF替换成OSAL_sizeof
- [x] 把所有的OSAL_OFFSETOF替换成OSAL_offsetof
- [x] 把所有的OSAL_CONTAINER_OF替换成OSAL_container_of

- [x] TEST的Run all tests移动到最后一个选项，并增加确认操作
- [x] TEST测试菜单中的返回键改成b，退出键改成q
- [x] 确定TEST退出是否清理所有的资源
- [x] TESTS新增CAN单独发送和单独接收测试用例
- [x] 输入q不能退出

- [x] include/autoconf.h应该加到.gitignore内
- [x] 增加make mrproper支持，清理kconfig
- [x] 确认clean和distclean是否重编了kconfig
- [x] make all的时候会重新触发gen config
- [x] 应该只在 .config 确实发生变化时才重新生成autoconf.h
- [x] make defconfig时没有生成.config 的提示，应该添加提示，输出.config生成路径才对
- [x] 修改 Makefile，让 defconfig 在静默模式下运行，只在 V=1 或出错时显示详细信息
- [x] 修改Config.in文件内的overlong错误
- [x] distclean没有删除.config.old
- [x] scripts/kconfig新增一个.gitignore文件，忽略中间产物
- [x] 优化make mrproper时清理kconfig的日志打印，不需要打印具体的删除操作
- [x] 考虑configs增加一级产品子目录，方便管理维护，进行配置时，直接使用正则匹配，从配置文件名内裁取子目录名
- [x] cmake目录移动到scripts内


- [ ] 把OSAL的平台相关的宏全都放到defconfig中定义
- [ ] 把OSAL的功能宏也移动到defconfig中
- [ ] CONFIG_OSAL_OS_POSIX改成被OS配置select
- [ ] 整理并统一生成头文件的路径
- [ ] buildroot paackage版本号支持custom，而不是固定写死，也不要用VERSION文件手动修改，直接就用custom覆盖
- [ ] 框架内部版本号改成类似buildroot和内核的形式，获取分支或git commit id的方式，然后添加编译时间，放到一个生成的头文件内，增加一个全局的版本宏定义支持引用
- [ ] 再增加一些版本信息打印宏以及测试用例，并验证软件内部软件宏功能是否正常
- [ ] 检查Makefile中所有的路径，禁止使用绝对路径，全都使用变量定义，兼容buildroot
- [ ] 验证所有的defconfig都可以正常清理、配置和编译，各个配置编译出的产物内容也都正确，并进行压力测试
- [ ] tests_full_x86实际没有编入可用测试用例

- [?] test --list可以考虑支持-m参数
- [ ] 把inittab的修改整理到笔记中
- [ ] 解决/run和/var的挂载问题，并确定tmpfs预留空间大小
- [ ] 解决/etc/init.d/S10mdev报错问题

- [ ] 筛查其他OSAL API的大小写规范问题
- [ ] 替换memcpy为OSAL的封装API
- [ ] OSAL_version.c重复了
- [ ] 确认OSAL_Printf是否支持输出到串口

- [ ] 优化core库的构建和链接机制
- [?] tests核心库部分移动到core内，然后将现在的products内的测试模块重新命名
- [ ] 开始验证hal层软件功能和设计合理性，包括数据结构设计、接口设计、文件组织
- [ ] core头文件安装支持配置
- [ ] hal层的公共头文件考虑分成api和config两部分，或者其他更好的组织形式，或者han_xxx_types.h考虑重命名成han_xxx_config.h
- [ ] 检查Kconfig中的项目全局配置项是否合理
- [ ] 从跨平台开发的角度分析当前代码实现还有哪些问题
- [ ] 新增一个内核驱动模块es-base.ko，支持系统分区表、HWID、启动分区记录
- [ ] ES-Middleware封装进程间通信机制（LPC）
- [ ] buildroot裁剪ti-firmware，直接上传产物，不再重新编译

- [ ] product内的库链接应该是根据defconfig进行配置的，而不是默认包含
- [ ] 确定gen_autoconf.sh是否有必要存在
- [ ] 把package/es-middleware/local.mk.example归档到笔记内

## 2. 其他项目
[[98.3.1 H200_100P_AM62x项目开发]]

## 3. 非紧急待办
[[98.2.1 非紧急待办|>>非紧急待办<<]]

## 4. 开发问题记录：
[[6.1.1 加载驱动报错| >>开发问题记录<<]]

