# 周期：【2026-05-30 (周六) ~ 2026-06-05 (周五)】

## 1. Task List

2026-05-30 星期六
- [x] 完善各个设备的消息类型定义 ✅ 2026-06-05
- [x] 重构PRL_MCU协议
- [x] 统一设备协议，使用DEV_TYPE字段区分设备
- [x] 创建PRL_MCU协议层模块
- [x] 修正PRL依赖关系和PDL_CCM实现问题
- [x] 添加PRL外设协议层单元测试用例
- [x] 添加PRL外设协议层

2026-05-31 星期日
- [x] 重构PCL和PDL，解决头文件导致的依赖混乱问题
- [x] 优化ACL、PDL、PCL接口设计
- [x] 重命名PCL->PConfig，ACL->AConfig，提供辨识度
- [x] 所有的AConfig枚举统一定义，统一管理，降低维护难度
- [x] 清理CCM产品内遗留的PMC代码
- [x] 统一PDL各个模块的命名规范，使用PDL_SUBMODULE-NAME_作为前缀 
- [x] 统一AConfig、HAL等各个模块内的宏定义命名，使用MODULE-NAME_SUBMODULE-NAME_作为前缀
- [x] 优化AConfig内业务功能与逻辑硬件的映射方式

2026-06-01 星期一
- [x] 优化PRL函数命名规范
- [x] 分离PRL对外API头文件和私有头文件
- [x] 重构PRL架构，从点对点协议重构为按设备类型组织
- [x] 新增场景化defconfig配置文件，主要新增各个模块的单元测试defconfig
- [x] 新增AConfig和PConfig的单元测试用例
- [x] 更新full_test_defconfig配置文件，启用所有模块的测试
- [x] OSAL内新增文件锁支持
- [x] HAL内硬件访问接口使用文件锁+互斥锁的形式，解决进程间和线程间的硬件并发访问控制问题
- [x] 优化文件锁定义方式，使用宏定义的形式，在OSAL内定义文件锁根目录，在模块内定义文件锁命名
- [x] 重构HAL层，移除HAL内所有的系统调用和标准库调用，统一使用OSAL封装的API
- [x] OSAL新增部分HAL依赖的API封装

2026-06-02 星期二
- [x] 重构CCM示例代码，协议相关部分统一使用PRL新版的API接口
- [x] PRL新增字节序处理支持
- [x] 修复PDL并发控制问题
- [x] 优化线程退出机制，解决线程资源泄露问题
- [x] HAL层CAN和串口增加通信超时控制
- [x] PDL_MCU增加设备状态机，用于监测MCU状态
- [x] PRL强制开启CRC校验，不再支持开启关闭
- [x] 优化HAL层宏定义和枚举命名规范
- [x] 简化测试框架，降低测试用例的开发和维护成本
- [x] 迁移测试框架到新的cmake构建系统
- [x] 测试框架适配kconfig机制，支持功能裁剪
- [x] 修复各个模块的测试编译问题
- [x] 新增PConfig测试用例
- [x] 优化构建脚本，使用完整的配置文件名，而不是内部进行拼接，降低学习成本
- [x] 清理PRL内的系统调用和标准库调用，统一使用OSAL的封装API

2026-06-03 星期三
- [x] 重构OSAL模块为SDK形式
- [x] 重构HAL模块为SDK形式
- [x] 重构PDL模块为SDK形式
- [x] 重构PRL模块为SDK形式
- [x] 重构PConfig模块为SDK形式
- [x] 重构AConfig模块为SDK形式
- [x] 重组目录结构，分离公共头文件和私有头文件
- [x] 移除废弃的兼容性代码

2026-06-04 星期四
- [x] 重构prl头文件组织形式，将prl的所有头文件在prl.h内包含
- [x] 重构hal头文件组织形式，将hal的所有头文件在hal.h内包含
- [x] 重构osal头文件组织形式，将osal的所有头文件在osal.h内包含
- [x] 重构core内核心模块，支持SDK形式导出头文件和库文件
- [x] 所有的错误码统一使用osa的定义，包括PRL和HAL
- [x] 移除各个模块内的错误码兼容性代码
- [x] 重构头文件路径
- [x] 重命名osal的内部头文件，统一添加_internal后缀，避免因为命名歧义导致误用
- [x] 重命名hal/include/config内的头文件，全部使用hal_开头，避免与外部头文件冲突
- [x] hal层的源文件应该按硬件类型划分下级目录，比如hal/src/linux/i2c/hal_i2c_linux.c，然后hal_i2c_internal.h也移动到hal/src/linux/i2c内
- [x] 所有的_internal头文件应该拆分到源码目录下，类似pdl当前的设计，而不是直接在src内增加一个include目录
- [x] osal_flock.c应该移动到posix中
- [x] 优化osal_flock.h路径
- [x] 检查有无非法使用其他模块私有头文件的情况
- [x] 删除scripts目录下的废弃脚本
- [x] 筛查并修复所有跨模块引用私有头文件的问题
- [x] 构建框架全局启用-Werror，确保不允许出现任何编译问题 ✅ 2026-06-05

2026-06-05 星期五
- [x] 去掉各个模块核心头文件的include命名空间，使用直接包含头文件的方式，方便IDE查找文件
- [x] hal层的Kconfig.linux和Kconfig.macos应该放到各自的目录内，而不是直接放在src
- [x] 创建osal和hal的test_defconfig
- [x] 优化defconfig文件命名规范，改成 **<product_name>** _ **<project_name>** _ **<module_name>** _ defconfig的形式
- [x] 对外公共头文件规范化，去掉api后缀，只需要在私有头文件加_internal后缀即可
- [x] 更新各个模块的头文件保护宏
- [x] osal_log的日志打印从printf改成write
- [x] 所有的枚举和常量宏定义全部改成16进制的形式
- [x] 重构OSAL日志系统，统一日志接口并优化性能

## [[README#98-2 非紧急待办|2. 其他待办]]
## [[README#98-3 项目开发|3. 其他项目开发]]
## [[README#6. 问题记录|4. 问题记录]]
