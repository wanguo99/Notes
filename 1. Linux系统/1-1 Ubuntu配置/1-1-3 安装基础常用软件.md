## 1. 安装基础工具
```bash
sudo apt install -y vim git curl wget  uuid-dev universal-ctags  # 安装基础工具
```
``` bash
sudo apt install -y tree unzip rsync # 安装基础工具
```
``` bash
sudo apt install -y python3 pip # 安装Python环境
```
``` bash
sudo apt install -y net-tools iputils-ping # 安装网络工具
```

## 2. 安装编译依赖：
```bash
sudo apt install -y  build-essential libncurses-dev cmake
```
```bash
sudo apt install -y  libgnutls28-dev uuid-dev libelf-dev bison flex
```

## [[1-1-3-1 安装便捷删除工具rf|3. 安装便捷删除工具rf]]

## [[97-3 vimrc|4. VIM配置]]

## 5. 安装VM-Tools【可选】：
```bash
sudo apt install -y open-vm-tools open-vm-tools-desktop
```
