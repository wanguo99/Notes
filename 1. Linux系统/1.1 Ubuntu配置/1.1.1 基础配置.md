# 1.1.1 基础配置

## 1. 设置 root 密码并切换至 root 【可选】
```bash
sudo passwd root && su
```

## 2. 配置时区
```bash
sudo timedatectl set-timezone Asia/Shanghai
```

## 3. 更新软件
```bash
sudo apt update -y && sudo apt upgrade -y 
```
